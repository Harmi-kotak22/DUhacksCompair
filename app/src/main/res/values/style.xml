<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Toolbar Title Style -->
    <style name="ToolbarTitleStyle">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/toolbar_title_color</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>


    <!-- Main Theme (Light Mode) -->
    <style name="Theme.Compair" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/lightPrimary</item>
        <item name="colorPrimaryVariant">@color/lightPrimaryVariant</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorBackground">@color/lightBackground</item>
        <item name="colorOnBackground">@color/black</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorAccent">@color/lightAccent</item>

        <!-- Status Bar & Navigation Bar -->
        <item name="android:statusBarColor">@color/lightPrimaryVariant</item>
        <item name="android:navigationBarColor">@color/lightBackground</item>
        <item name="android:windowBackground">@color/lightBackground</item>
    </style>

    <!-- Dark Mode Theme -->
    <style name="Theme.Compair.Dark" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/darkPrimary</item>
        <item name="colorPrimaryVariant">@color/darkPrimaryVariant</item>
        <item name="colorAccent">@color/darkAccent</item>
        <item name="colorBackground">@color/darkBackground</item>
        <item name="colorOnBackground">@color/white</item>
        <item name="colorSurface">@color/darkBackground</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="android:windowBackground">@color/darkBackground</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Profile Label Style -->
    <style name="ProfileLabel">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginTop">12dp</item>
    </style>

    <!-- Profile EditText Style -->
    <style name="ProfileEditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:background">@drawable/edit_text_background</item>
        <item name="android:padding">12dp</item>
        <item name="android:textColorHint">@color/black</item>
    </style>

    <!-- Rounded Image View (Fixed for API 29) -->
    <style name="RoundedImageView">
        <item name="android:layout_width">100dp</item>
        <item name="android:layout_height">100dp</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:background">@drawable/rounded_logo</item>
    </style>

    <style name="SpinnerTextStyle">
        <item name="android:textColor">@color/black</item> <!-- Text color -->
        <item name="android:textSize">16sp</item> <!-- Text size -->
    </style>


</resources>
